spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medalj
    username: medalj
    password: medalj

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

  cloud:
    azure:
      storage:
        blob:
          endpoint: http://127.0.0.1:10000/devstoreaccount1
          account-name: devstoreaccount1
          account-key: Eby8vdM02xNOcqFeqCnf2g==

app:
  medals:
    image-container: medal-pictures
  blob:
    seed:
      enabled: true

server:
  port: 8080


